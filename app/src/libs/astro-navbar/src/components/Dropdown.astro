---
interface Props {
    class?: string;
}
const { class: className, ...rest } = Astro.props;
---


<menu class:list={["astronav-dropdown dont-close", className]} {...rest} aria-expanded="false"><slot/></menu>
